<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruomeng.onlineorderingbackend.mapper.CustomerMapper">

    <!-- 分页查询顾客 -->
    <select id="pageQuery" resultType="com.ruomeng.onlineorderingbackend.model.entity.Customer">
        SELECT * FROM customer
        <where>
            <if test="nickname != null and nickname != ''">
                AND nickname LIKE CONCAT('%', #{nickname}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="isMember != null">
                AND is_member = #{isMember}
            </if>
        </where>
        ORDER BY update_time DESC
    </select>

    <!-- 更新顾客状态 -->
    <update id="updateStatus">
        UPDATE customer 
        <set>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="isMember != null">
                is_member = #{isMember},
            </if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <!-- 更新顾客积分 -->
    <update id="updatePoints">
        UPDATE customer 
        SET points = #{points}, 
            total_points = #{totalPoints}, 
            update_time = #{updateTime} 
        WHERE id = #{id}
    </update>

    <!-- 更新用户信息 -->
    <update id="updateInfo">
        UPDATE customer 
        <set>
            <if test="nickname != null and nickname != ''">
                nickname = #{nickname},
            </if>
            <if test="avatar != null and avatar != ''">
                avatar = #{avatar},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

</mapper>
