<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruomeng.onlineorderingbackend.mapper.SetmealDishMapper">

    <!-- 批量插入套餐菜品关系 -->
    <insert id="insertBatch">
        INSERT INTO setmeal_dish (setmeal_id, dish_id, copies, create_time, update_time)
        VALUES
        <foreach collection="setmealDishes" item="item" separator=",">
            (#{item.setmealId}, #{item.dishId}, #{item.copies}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

</mapper>
