<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruomeng.onlineorderingbackend.mapper.CategoryMapper">

    <!-- 插入分类 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, type, sort, status, create_time, update_time)
        VALUES (#{name}, #{type}, #{sort}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <!-- 更新分类 -->
    <update id="update">
        UPDATE category
        SET name = #{name},
            sort = #{sort},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 更新分类状态 -->
    <update id="updateStatus">
        UPDATE category
        SET status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 分页查询分类 -->
    <select id="pageQuery" resultType="com.ruomeng.onlineorderingbackend.model.entity.Category">
        SELECT * FROM category
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="type != null">
                AND type = #{type}
            </if>
        </where>
        ORDER BY sort ASC, update_time DESC
    </select>

</mapper>
