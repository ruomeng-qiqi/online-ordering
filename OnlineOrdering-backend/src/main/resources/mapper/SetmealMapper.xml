<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruomeng.onlineorderingbackend.mapper.SetmealMapper">

    <!-- 插入套餐 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO setmeal (name, category_id, price, image, description, status, create_time, update_time)
        VALUES (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <!-- 更新套餐 -->
    <update id="update">
        UPDATE setmeal
        SET name = #{name},
            category_id = #{categoryId},
            price = #{price},
            image = #{image},
            description = #{description},
            status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 批量删除套餐 -->
    <delete id="deleteBatchByIds">
        DELETE FROM setmeal WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 更新套餐状态 -->
    <update id="updateStatus">
        UPDATE setmeal
        SET status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 分页查询套餐 -->
    <select id="pageQuery" resultType="com.ruomeng.onlineorderingbackend.model.entity.Setmeal">
        SELECT * FROM setmeal
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY update_time DESC
    </select>

</mapper>
